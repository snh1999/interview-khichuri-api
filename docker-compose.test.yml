services:
  test-db:
    image: postgres:16-alpine
    ports:
      - "5433:5432"
    environment:
      POSTGRES_USER: admin_user
      POSTGRES_PASSWORD: password1234
      POSTGRES_DB: interview_khichuri_test
    tmpfs:
      - /var/lib/postgresql/data
